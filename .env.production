# Production API URL — points to the Render-hosted backend
# This is baked into the Vite bundle at build time.
# ALSO set this in your Vercel project: Settings → Environment Variables → VITE_API_URL
VITE_API_URL=https://gharpayy-arena-api.onrender.com/api
